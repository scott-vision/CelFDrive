# Auto detect text files and perform LF normalization
* text=auto

# Large binary model and microscopy example assets
*.pt filter=lfs diff=lfs merge=lfs -text
examples/max_projection_sahi/**/*.tif filter=lfs diff=lfs merge=lfs -text
