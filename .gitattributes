# Auto detect text files and perform LF normalization
* text=auto

# Large binary model assets
*.pt filter=lfs diff=lfs merge=lfs -text

# Reproducibility benchmark images; source-only clones retain LFS pointers.
benchmarks/data/cellclicker/cellcognition_h2b_P0037/images/*.png filter=lfs diff=lfs merge=lfs -text
